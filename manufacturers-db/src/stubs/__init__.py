"""Connector stubs scheduled for future sprints.

Each stub is a `BaseConnector` subclass with `fetch()` raising NotImplementedError
plus a docstring describing the API endpoint and what fields it provides. This
keeps the import surface consistent and makes the next sprint a fill-in-the-blank
exercise instead of a design exercise.
"""
